# Workspace App Template

This folder is deprecated as the primary scaffold source.

Use the standalone internal package instead:

- package: [packages/workspace-template](../../packages/workspace-template/README.md)
- Electron starter app: [packages/workspace-template/starter](../../packages/workspace-template/starter/package.json)
- scaffold plugin target: `plugins/xingularity-workspace-template-scaffold`

The old example files remain here only as compatibility references while the package/plugin flow replaces the copy-by-reference template workflow.
